{
  "$id": "backup-v4.3.1",
  "$schema": "https://json-schema.org/draft-07/schema",
  "type": "object",
  "properties": {
    "AccountId": {
      "type": "string",
      "description": "ID of the AWS account the stack is deployed to. Terraform refuses to run against any other account."
    },
    "AwsProviderVersion": {
      "type": "string",
      "description": "Version constraint for the `hashicorp/aws` Terraform provider.",
      "default": "\u003e= 6.0.0, \u003c 7.0.0"
    },
    "Environment": {
      "type": "string",
      "description": "Environment name (for example `dev` or `prod`), applied as the `Environment` tag on every resource in the stack."
    },
    "IamForCicd": {
      "type": "object",
      "description": "Deprecated: use the `cicd-common` template instead. `AssumableCdRole` lets the AWS provider assume the environment's CI/CD role, so you can run Terraform locally with the same permissions as the pipeline.",
      "default": {
        "AssumableCdRole": false
      },
      "properties": {
        "AssumableCdRole": {
          "type": "boolean",
          "default": false
        }
      },
      "required": [
        "AssumableCdRole"
      ]
    },
    "IamForCicd.AssumableCdRole": {
      "type": "boolean",
      "description": "Override single parameter of IamForCicd",
      "default": false
    },
    "IncludeLockFile": {
      "type": "boolean",
      "description": "Include a Terraform dependency lock file (`.terraform.lock.hcl`) that pins provider versions.",
      "default": false
    },
    "NotifySlack": {
      "type": "object",
      "description": "Not needed: backup notifications are set up centrally when the AWS account is created. Sends backup success and failure events to a Slack channel. The webhook URL is read from the SSM parameter `/\u003cEnvironment\u003e/slack/webhook/backup`. The parameter is created with a placeholder value, so set the real URL after the first apply.",
      "default": {
        "Enable": false
      },
      "properties": {
        "Enable": {
          "type": "boolean",
          "default": false
        }
      },
      "required": [
        "Enable"
      ]
    },
    "NotifySlack.Enable": {
      "type": "boolean",
      "description": "Override single parameter of NotifySlack",
      "default": false
    },
    "Region": {
      "type": "string",
      "description": "AWS region the stack's resources are created in.",
      "default": "eu-west-1"
    },
    "S3Backend": {
      "type": "boolean",
      "description": "Store Terraform state in the environment's S3 state bucket, with state locking. Requires the `remote-state` template to have created the bucket; does not create it. If disabled, state is stored locally.",
      "default": true
    },
    "StackName": {
      "type": "string",
      "description": "Name of the Terraform stack, normally the name of the stack's folder in the IaC repository. Determines where the stack's state is stored."
    },
    "Team": {
      "type": "string",
      "description": "Team name, applied as the `Team` tag on every resource in the stack."
    },
    "TerraformVersion": {
      "type": "string",
      "description": "Terraform version constraint, set as `required_version` for the stack.",
      "default": "\u003e= 1.10.0"
    },
    "Terragrunt": {
      "type": "object",
      "description": "Generate a `terragrunt.hcl` so the stack can be run with Terragrunt instead of plain Terraform. This is an experimental feature and should not be used in production.",
      "default": {
        "Enable": false
      },
      "properties": {
        "Enable": {
          "type": "boolean",
          "default": false
        }
      },
      "required": [
        "Enable"
      ]
    },
    "Terragrunt.Enable": {
      "type": "boolean",
      "description": "Override single parameter of Terragrunt",
      "default": false
    },
    "UseResourceSelection": {
      "type": "boolean",
      "description": "Select resources to back up by listing their ARNs explicitly, in the `selection_resources` local (override it in `config_override.tf`; defaults to an empty list). Can be combined with `UseTagSelection`.",
      "default": false
    },
    "UseTagSelection": {
      "type": "boolean",
      "description": "Select resources to back up by tag: every resource tagged `Environment=\u003cEnvironment\u003e` is included in the backup plan. Resources tagged `backup-enabled=false` and the Datadog Lambda forwarder (from the `datadog-common` template) are excluded.",
      "default": true
    }
  },
  "required": [
    "StackName"
  ]
}
